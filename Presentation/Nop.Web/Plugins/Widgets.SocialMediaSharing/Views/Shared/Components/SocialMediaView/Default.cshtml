
@using Nop.Web.Framework.Models.DataTables
@model List<SocialMediaHomeModel>

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Share Modal</title>

    <style>

        body {
            font-family: Arial, sans-serif;
        }

        button {
            margin: 20px;
            padding: 10px 20px;
            font-size: 16px;
        }

        .modal {
            display: none;
            position: fixed;
            z-index: 1;
            left: 0;
            top: 0;
            width: 100%;
            height: 100%;
            overflow: auto;
            background-color: rgb(0,0,0);
            background-color: rgba(0,0,0,0.4);
            padding-top: 60px;
        }

        .modal-content {
            background-color: #fefefe;
            margin: 5% auto;
            padding: 20px;
            border: 1px solid #888;
            width: 80%;
        }

        .close {
            color: #aaa;
            float: right;
            font-size: 28px;
            font-weight: bold;
        }

            .close:hover,
            .close:focus {
                color: black;
                text-decoration: none;
                cursor: pointer;
            }

        .share-options a {
            display: flex;
            align-items: center;
            margin: 10px 0;
            text-decoration: none;
            color: black;
        }

        .share-options img {
            width: 20px;
            height: 20px;
            margin-right: 10px;
        }

        input[type="text"] {
            width: calc(100% - 22px);
            padding: 10px;
            margin: 10px 0;
            box-sizing: border-box;
        }

        #copyBtn {
            padding: 10px 20px;
        }



    </style>

   
</head>
<body>
    <button class="btn btn-primary" id="shareBtn"><i class="bi bi-share-fill"></i>Share</button>

    <div id="shareModal" class="modal">
        <div class="modal-content">
            <span class="close">&times;</span>
            <h3>Share advert</h3>
            <div class="share-options">
                <div class="developer-container">
                    @foreach (var media in Model)
                    {
                        <div class="developer-item card">
                            <div class="card-body">

                                <div>

                                 @{
                                     var data = new ViewDataDictionary(ViewData)
                                    {
                                         {"pictureUrl",media.Icon.ThumbImageUrl}
                                    };

                                     @await Html.PartialAsync("~/Plugins/Widgets.SocialMediaSharing/Views/Shared/SocialMediaList.cshtml", data);

                                   }
                                </div>

                                <div class="card-text">
                                    <h5 class="name">@media.Name</h5>
                                </div>
                            </div>
                        </div>


                    }
                </div>
 
            </div>
            <input type="text" value="https://example.com" id="shareLink">
            <button id="copyBtn">Copy</button>
        </div>
    </div>

    <script>
        // Get the modal
        var modal = document.getElementById("shareModal");

        // Get the button that opens the modal
        var btn = document.getElementById("shareBtn");

        // Get the <span> element that closes the modal
        var span = document.getElementsByClassName("close")[0];

        // When the user clicks the button, open the modal
        btn.onclick = function () {
            modal.style.display = "block";
        }

        // When the user clicks on <span> (x), close the modal
        span.onclick = function () {
            modal.style.display = "none";
        }

        // When the user clicks anywhere outside of the modal, close it
        window.onclick = function (event) {
            if (event.target == modal) {
                modal.style.display = "none";
            }
        }

        // Copy link to clipboard
        var copyBtn = document.getElementById("copyBtn");
        var shareLink = document.getElementById("shareLink");

        copyBtn.onclick = function () {
            shareLink.select();
            document.execCommand("copy");
            alert("Link copied to clipboard!");
        }
    </script>
</body>
</html>























@* 


@{
    var cnt = 0;
}


<div class="developer-container">
    @foreach(var media in Model)
    {
        <div class="developer-item card">
            <div class="card-body">
                    
                    <div>
                    
                        @{
                            var data = new ViewDataDictionary(ViewData)
                            {
                                {"pictureUrl",media.Icon.ThumbImageUrl}
                            };
                        @await Html.PartialAsync("~/Plugins/Widgets.SocialMediaSharing/Views/Shared/SocialMediaList.cshtml", data);
                        
                        }
                    </div>

                <div class="card-text">
                    <h5 class="name">@media.Name</h5> 
                </div>


            </div>
        </div>


    }
</div>




<style>

    .developer-container {
        display: flex;
        flex-wrap: wrap;
        gap: 20px;
        justify-content: flex-start;
    }

    .developer-item {
        width: 225px;
        box-sizing: border-box;
        padding: 15px;
        text-align: center;
        border: 1px solid #ddd;
        border-radius: 3px;
        box-shadow: 0 6px 12px rgba(0, 0, 0, 0.1);
        background-color: #fff;
        overflow: hidden;
        transition: box-shadow 0.3s ease-in-out;
    }

        .developer-item:hover {
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.15);
        }

    .card-body {
        padding: 0;
    }

    .slider-wrapper.theme-custom {
        width: 100%;
        margin-bottom: 10px;
    }

    .card-text {
        text-align: center;
        padding-top: 10px;
    }

    .name {
        font-size: 18px;
        font-weight: bold;
        margin-bottom: 5px;
        color: #333;
    }

    .designation {
        font-size: 16px;
        color: #666;
        margin-bottom: 5px;
    }

    .badge-container {
        display: flex;
        justify-content: center;
        align-items: center;
        gap: 10px; /* Adjust the gap between badges as needed */
        margin-top: 10px; /* Add margin if needed */
    }

    .badge {
        display: flex;
        align-items: center;
        gap: 5px; /* Adjust the gap between the image and the text */
    }

</style> 
*@